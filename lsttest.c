#include "libft.h"
#include <stdio.h>
t_list	*ft_lstappend(t_list *list, t_list *new);

int	main(void)
{
	t_list	*head;

	head->content = "this is the first";
	head->content_size = ft_strlen(head->content);
	return (0);
}
